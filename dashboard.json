{
    "dashboard": {
      "id": null,
      "uid": null,
      "title": "Predictions by Model",
      "tags": ["api", "predictions"],
      "timezone": "browser",
      "schemaVersion": 27,
      "version": 0,
      "refresh": "5s",
      "panels": [
        {
          "id": 1,
          "type": "timeseries",
          "title": "Predictions per Model",
          "datasource": "victoriametrics",
          "targets": [
            {
              "refId": "A",
              "expr": "sum by (model_id) (rate(oreo_mlserve_predictions_total[5m]))",
              "legendFormat": "{{model_id}}"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "ops",
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "green", "value": null },
                  { "color": "orange", "value": 100 },
                  { "color": "red", "value": 500 }
                ]
              }
            },
            "overrides": []
          },
          "gridPos": { "h": 10, "w": 24, "x": 0, "y": 0 }
        }
      ]
    },
    "folderId": 2431,
    "overwrite": true
  }